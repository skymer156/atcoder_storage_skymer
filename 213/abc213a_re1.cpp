#include <bits/stdc++.h>

#define rep(i,n) for (int i = 0; i < (n); ++i)

using namespace std;

int main(){
    int A, B;
    cin >> A >> B;

    int ans = A^B;
    cout << ans << endl;

    return 0;
}